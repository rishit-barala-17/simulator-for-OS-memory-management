#include<bits/stdc++.h>
#define f(v) (v).begin(), (v).end()
#define in(v) for(auto &x : v) cin >> x;     // in(vector_name)   // automatically takes full input
#define maxi(a,b) a = max(a,b)
#define mini(a,b) a = min(a,b)
#define pb push_back

using namespace std;
void solve(){
		
	
	
		
}
int main(){
	
	int t;
	cin>>t;
	
	while(t--)
	{
		solve() ;
		
	}
	
		
	return 0;
}